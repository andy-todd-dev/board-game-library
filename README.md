# board-game-library

## Prerequisites

### Kubernetes Configuration

A valid `kubeconfig` file must be manually copied into the dev container before using any Kubernetes features. The container does not automatically inherit the host's kube config.
